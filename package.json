{
  "name": "gowa-manager",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "gowa-manager": "./bin/npx-cli.js"
  },
  "files": [
    "bin/npx-cli.js"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "build:tsc": "tsc --noEmit",
    "build:client": "cd client && bun run build",
    "embed-static": "bun run scripts/embed-static.ts",
    "prebuild:production": "bun run build:client && bun run embed-static",
    "build:production": "bun run prebuild:production && bun run build",
    "dev:server": "bun --watch run src/index.ts",
    "dev:client": "cd client && bun run dev",
    "dev": "cd client && bun run build:watch & bun run scripts/dev-watch.ts",
    "start": "bun run src/index.ts",
    "compile": "bun run build:production && bun build --compile --define 'process.env.NODE_ENV=\"production\"' --target bun --outfile gowa-manager src/index.ts",
    "compile:optimized": "bun run build:production && bun build --compile --minify --bytecode --define 'process.env.NODE_ENV=\"production\"' --target bun --outfile gowa-manager-opt src/index.ts"
  },
  "devDependencies": {
    "@elysiajs/cors": "^0.8.0",
    "@elysiajs/static": "^1.3.0",
    "adm-zip": "^0.5.16",
    "elysia": "^1.3.21",
    "node-cron": "^3.0.3",
    "pidusage": "^4.0.1",
    "ws": "^8.18.3",
    "@faker-js/faker": "^9.9.0",
    "@types/bun": "latest",
    "tree-kill": "^1.2.2",
    "typescript": "^5.9.2"
  }
}
